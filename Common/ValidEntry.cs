using AuthUtility.Constants;
using System.Linq;

namespace AuthUtility.Common
{
    public class ValidEntry
    {
        public static bool IsValid(string value)
        {
            //if (string.IsNullOrWhiteSpace(value))
            //    return false;

            //return (value.Split(',').Count() <= UtilityConstants.MaxUserIdCount) && !value.Any(char.IsLetter) ? true : false;
            return default;
        }
    }
}